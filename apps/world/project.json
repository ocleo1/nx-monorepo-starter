{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "apps/world",
  "sourceRoot": "apps/world/src",
  "projectType": "application",
  "targets": {
    "build": {
      "dependsOn": ["^build", "^build:css"]
    },
    "build:dev": {
      "dependsOn": ["^build:dev", "^build:css"]
    }
  }
}
